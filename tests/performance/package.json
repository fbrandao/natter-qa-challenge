{
    "name": "performance",
    "version": "1.0.0",
    "private": true,
    "type": "commonjs",
    "scripts": {
        "load:record": "artillery run ./join-call.ts --record --key $ARTILLERY_API_KEY",
        "load": "artillery run --output ../../reports/performance/results-performance.json ./join-call.ts"    
    }
}

  